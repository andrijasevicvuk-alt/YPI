# Manual Entry

Manual entry is the first guaranteed input path.

Initial goal:

- capture worker-entered listing data
- preserve the entered payload in raw ingestion tables
- attach a source registry record even for manual data
- keep normalization and scoring out of this boundary
