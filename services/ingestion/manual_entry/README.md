# Manual Entry

Manual entry is a bootstrap/admin input path, not the core product flow.

Initial goal:

- capture controlled admin-entered listing data for bootstrapping, testing, or exceptional corrections
- preserve the entered payload in raw ingestion tables
- attach a source registry record even for manual data
- keep normalization and scoring out of this boundary
