# CSV Import

CSV import is available only as a controlled bootstrap/admin path.

Important constraints:

- do not assume any confirmed external dataset
- do not shape the data model around one CSV layout
- map source columns into raw ingestion payloads first
- do not treat CSV upload as the main data acquisition model
