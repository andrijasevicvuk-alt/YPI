# Source Registry

Every acquisition path must have a source registry record from day one.

Examples:

- future_marketplace_pilot
- future_broker_pilot
- internal_manual_entry
- controlled_csv_upload

The source registry stores trust and collection metadata independently from parser or scoring logic.
