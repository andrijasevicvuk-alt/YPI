{
  "name": "yacht-premium-insurance",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @ypi/web",
    "build:web": "npm run build --workspace @ypi/web",
    "lint:web": "npm run lint --workspace @ypi/web",
    "typecheck": "npm run typecheck --workspace @ypi/web && npm run typecheck --workspace @ypi/domain && npm run typecheck --workspace @ypi/data-access && npm run typecheck --workspace @ypi/scoring"
  }
}
